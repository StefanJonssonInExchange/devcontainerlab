{
  "_comment": "Copy to devcontainer.local.json and fill in your values. This file is gitignored.",
  "OPENCODE_MODEL": "opencode/big-pickle",
  "OPENCODE_PROMPT": "The dotnet test command shows a failing unit test. Fix it.",
  "ANTHROPIC_API_KEY": "sk-ant-... (only needed for anthropic/* models)"
}
